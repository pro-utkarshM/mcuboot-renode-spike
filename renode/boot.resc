# Create the path-neutral Renode machine. The controller gives every session a
# private runtime directory, then uses ordered CLI -e commands to attach its
# UART PTY/backend, load its exact flash input, begin tracing, and start.

mach create "ota"
machine LoadPlatformDescription @/workspace/app/renode/platform.repl
