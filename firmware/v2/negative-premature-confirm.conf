# Intentionally unsafe verifier-sensitivity variant.  It confirms the image
# before its v2 migration state is durable and must fail fault-injection tests.
CONFIG_SPIKE_V2_NEGATIVE_PREMATURE_CONFIRM=y
