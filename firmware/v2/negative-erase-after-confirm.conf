# Intentionally unsafe verifier-sensitivity variant. It establishes durable
# state, confirms v2, and then deletes the state that confirmation promised.
CONFIG_SPIKE_V2_NEGATIVE_ERASE_AFTER_CONFIRM=y
